import { IDToBirthPipe } from './id-to-birth.pipe';

describe('IDToBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new IDToBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
